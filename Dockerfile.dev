FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173

# Adjust command if your dev script is different (e.g. "npm run dev -- --host")
CMD ["npm", "run", "dev", "--", "--host"]