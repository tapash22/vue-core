services:
  app-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: my-app-dev
    ports:
      - '5173:5173'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true # Guarantees live reloading across Windows WSL2
